var fs = require('fs');

module.exports = function() {
	const available = fs
		.readdirSync(__dirname)
		.filter(function(item) {
			return /^[^.]+$/.test(item);
		})
	return JSON.stringify(available);
};
