/**
 * @description
 *
 * Check if the value is alphanumeric
 *
 * The current regexp also matches empty string,
 * if empty string should not be allowed: /^[a-zåäöA-ZÅÄÖ0-9/\-_+]+$/
 *
 * @param {String} value - input value
 *
 * @return {Boolean}
 */
const isAlphaNumeric = value => (/^[a-zåäöA-ZÅÄÖ0-9/\-_+]*$/).test(value);

export default isAlphaNumeric;
