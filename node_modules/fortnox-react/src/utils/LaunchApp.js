let timerID = 0;
const asyncInit = (fnInitCallback) => {
	if (document.readyState === 'complete') {
		clearTimeout(timerID);
		fnInitCallback();
	} else {
		timerID = setTimeout(() => asyncInit(fnInitCallback), 1);
	}
};

export default (fnInitCallback) => {
	asyncInit(fnInitCallback);
};
