import AppContainer from './AppContainer';

export default () => {
	if (AppContainer.isFramedContext()) {
		return AppContainer.publish({ type: 'unauthorized-v1' });
	}
	window.location.href = '/fs/fs/login.php';
	return null;
};
