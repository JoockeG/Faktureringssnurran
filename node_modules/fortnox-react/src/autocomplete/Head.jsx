import React from 'react';
import PropTypes from 'prop-types';

const AutocompleteTableHead = ({ children }) => (
	<thead>
		<tr>{children}</tr>
	</thead>
);

AutocompleteTableHead.propTypes = {
	children: PropTypes.node.isRequired
};

export default AutocompleteTableHead;
