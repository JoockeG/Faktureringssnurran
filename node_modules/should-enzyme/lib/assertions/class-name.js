(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['./assertion-builder'], factory);
  } else if (typeof exports !== "undefined") {
    factory(require('./assertion-builder'));
  } else {
    var mod = {
      exports: {}
    };
    factory(global.assertionBuilder);
    global.className = mod.exports;
  }
})(this, function (_assertionBuilder) {
  'use strict';

  (0, _assertionBuilder.assertionBuilder)('className', function (expected) {
    return this.hasClass(expected);
  }, function (expected) {
    return 'expected \'' + this.name() + '\' to have className \'' + expected + '\' but found \'' + this.classNames() + '\'';
  });
});